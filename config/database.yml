# SQLite. Versions 3.8.0 and up are supported.
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'
#default: &default
#adapter: postgresql
#encoding: unicode
#pool: <%#= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
#timeout: 5000
#port: 5432
#default: &default
#  adapter: postgresql
#  encoding: unicode
#  pool: <%#= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
#  timeout: 5000
#  port: 5432
default: &default
  adapter: postgresql
  encoding: unicode
  # For details on connection pooling, see Rails configuration guide
  # http://guides.rubyonrails.org/configuring.html#database-pooling
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: revival_recipe_development
  username: <%= ENV['REVIVAL_RECIPE_DEVELOPMENT_DATABASE_USERNAME'] %>
  password: <%= ENV['REVIVAL_RECIPE_DEVELOPMENT_DATABASE_PASSWORD'] %>
  host: localhost
  port: 5432
  # Connect on a TCP socket. Omitted by default since the client uses a
  # domain socket that doesn't need configuration. Windows does not have
  # domain sockets, so uncomment these lines

  # The TCP port the server listens on. Defaults to 5432.
  # If your server runs on a different port number, change accordingly.

  # Schema search path. The server defaults to $user,public
  #schema_search_path: myapp,sharedapp,public

  # Minimum log levels, in increasing order:
  #   debug5, debug4, debug3, debug2, debug1,
  #   log, notice, warning, error, fatal, and panic
  # Defaults to warning.
  #min_messages: notice

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
 <<: *default
  database: test_db
  username: postgres
  password: <%= ENV['TEST_DB_DATABASE_PASSWORD'] %>

 production:
  <<: *default
#  timeout: 5000
#  port: 5432
  database: revival_recipe_production
  username: <%= ENV['REVIVAL_RECIPE_PRODUCTION_DATABASE_USERNAME'] %>
  password: <%= ENV['REVIVAL_RECIPE_PRODUCTION_DATABASE_PASSWORD'] %>